export default function LeadsPage() {
  return (
    <main className="mx-auto max-w-6xl px-8 py-8">
      <header className="mb-6">
        <h1 className="text-2xl font-semibold tracking-tight">Leads &amp; MQLs</h1>
        <p className="text-sm text-gray-500">Lead generation & conversion</p>
      </header>
      <div className="rounded-xl border border-dashed border-gray-300 bg-white p-10 text-center">
        <p className="text-sm font-medium text-gray-700">Coming soon</p>
        <p className="mx-auto mt-2 max-w-md text-sm text-gray-500">
          MQLs (form submissions, gated asset downloads, abandoned-cart MQLs),
          lead conversion rate, landing-page conversion ranking, and form
          submission / abandonment. Built on the same normalized metric store —
          scheduled after the E-Commerce cluster.
        </p>
      </div>
    </main>
  );
}
